// Declare the package location for version tracking utility
package com.grubhelper.util;

/**
 * Utility class holding application version constants and remote manifest URLs.
 */
public class Version {
    // Constant string holding the current application version
    public static final String CURRENT_VERSION = "1.0.0";

    // Constant string holding the URL to check for remote version updates
    public static final String UPDATE_URL = "https://raw.githubusercontent.com/grub-helper/grub-helper/main/version.json";
}
